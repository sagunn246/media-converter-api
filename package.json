{
  "name": "media-converter-root",
  "version": "1.0.0",
  "private": true,
  "description": "Production-ready Media Converter application with separated REST API and Next.js Frontend",
  "scripts": {
    "dev": "concurrently \"npm --prefix api run dev\" \"npm --prefix application run dev\"",
    "dev:api": "npm --prefix api run dev",
    "dev:app": "npm --prefix application run dev",
    "start:api": "npm --prefix api run start",
    "start:app": "npm --prefix application run start",
    "build:app": "npm --prefix application run build",
    "build": "npm run build:app"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
